@model IEnumerable<EncuestaV2.Data.UsuarioRespuestas>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin2.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PreguntaRespuestas.Texto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Preguntas.Texto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Usuarios.Nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PreguntaRespuestas.Texto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Preguntas.Texto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Usuarios.Nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdUsuarioRespuestas }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdUsuarioRespuestas }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdUsuarioRespuestas })
        </td>
    </tr>
}

</table>
